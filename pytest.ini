[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra

# 경고는 일부러 숨기지 않는다.
#  - datetime.utcnow() deprecation  -> src/models.py:13
#  - Query.get() LegacyAPIWarning   -> src/db.py:79
# 둘 다 실제 코드 부채이므로 별도 단계에서 처리한다.
